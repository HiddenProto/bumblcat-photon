{
  "name": "bumblcat-photon",
  "version": "1.0.0",
  "description": "bumblcat-photon — server-side TLS proxy worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
